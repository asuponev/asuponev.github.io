import type { Config } from 'tailwindcss'

export default <Partial<Config>>{
  theme: {
    fontFamily: {
      sans: ['Space Grotesk', 'sans-serif'],
    },
  },
  darkMode: 'class',
  plugins: [],
  content: [],
}
